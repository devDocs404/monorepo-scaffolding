# === Database Configuration ===

# Full connection string (used by ORMs or other DB tools)
DATABASE_URL="mysql://<DB_USER>:<DB_PASSWORD>@<DB_HOST>:<DB_PORT>/<DB_NAME>"

# Individual credentials (used by DrizzleKit or custom DB clients)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_db_password
DB_NAME=refrags

# === Auth Configuration ===

# Secret used for signing/verifying tokens (use a long, random string in production)
BETTER_AUTH_SECRET=your_auth_secret

# URL of your authentication service or Hono server
BETTER_AUTH_URL=http://localhost:3000

# === App Configuration ===

# CORS origin (your frontend dev server or production domain)
CORS_ORIGIN=http://localhost:3001
